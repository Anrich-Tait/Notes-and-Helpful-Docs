// Purpose: To declare and output two variables, one for age and one for a name.

#include <stdio.h>
#include <string.h>

int main() {
  int myAge = 23;
  char myName[] = "Main Character";

  printf("%i\n", myAge);
  printf("%s\n", myName);
  return 0;
}
